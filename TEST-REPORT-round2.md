# Test Round 2 — Full Test Report (2026-08-13)

**স্ট্যাটাস: সব পাওয়া বাগ ফিক্স করা হয়েছে ✅ (PR #70)**

পরীক্ষার ধরন: সব রুট HTTP টেস্ট, প্রোডাকশন বিল্ড, ইউনিট টেস্ট, i18n সম্পূর্ণতা (English মোড), ব্রাউজার-লেভেল প্যাটার্ন অ্যানালাইসিস।

---

## ✅ পাস করেছে — কোনো সমস্যা নেই

| পরীক্ষা | ফলাফল |
|---|---|
| সব রুট (২৮টা পেজ) HTTP 200 | ✅ `/`, `/donors`, `/requests`, `/blood-seekers`, `/admin`, `/dashboard` সহ সব |
| প্রোডাকশন বিল্ড (`next build`) | ✅ Exit 0 — ৫৮টা পেজ সফল, কোনো এরর নেই |
| ইউনিট টেস্ট (`vitest`) | ✅ ৫ ফাইল, ৫৮/৫৮ টেস্ট পাস |
| TypeScript (`tsc --noEmit`) | ✅ কোনো এরর নেই |
| ESLint | ✅ বিল্ডে শুধু `<img>` পারফরম্যান্স ওয়ার্নিং (বাগ নয়) |
| হোমপেজের সব অভ্যন্তরীণ লিংক (২২টা) | ✅ সব OK, কোনো ৪০৪ নেই |
| `t()` ডটেড কী (১০২টা ব্যবহার) | ✅ সব ডিকশনারিতে আছে, মিসিং ০টা |
| পাবলিক কার্ড (RequestCard, RequesterCard, DonorCard, LiveSeekers, BloodSeekers) | ✅ পুরোপুরি বাইলিঙ্গুয়াল (`en ? ... : ...` প্যাটার্ন) |
| StatusBadge, LiveBadge | ✅ `t()` দিয়ে ট্রান্সলেট হয় |
| অ্যাডমিন পেজ অথ গার্ড | ✅ লগইন ছাড়া ডেটা লোড হয় না, `/login`-এ রিডাইরেক্ট |
| ছবি (Supabase/Cloudinary) | ✅ `<img>` ব্যবহৃত — `next/image` remotePatterns সমস্যা নেই |
| ডেভ সার্ভার লগ | ✅ কোনো runtime এরর নেই |

---

## 🔴 বাগ #1 — English মোডে ১৫টা স্ট্রিং বাংলাই থাকে (ট্রান্সলেশন মিসিং) — **FIXED ✅**

**কোথায়:** `src/lib/i18n-dict/` ডিকশনারিতে এন্ট্রি নেই, কিন্তু কোডে `tr()`/`tx()` দিয়ে পাঠানো হয় — তাই `toEnglish()` মিস করে, English মোডেও বাংলা টেক্সট দেখায়।

| স্ট্রিং | ফাইল |
|---|---|
| "ফোন নম্বরের গোপনীয়তা" (শিরোনাম) | privacy/page.tsx |
| "যোগাযোগের ক্লিক লগ ও IP হ্যাশিং" (শিরোনাম) | privacy/page.tsx |
| "তথ্য সংরক্ষণের মেয়াদ (Retention)" (শিরোনাম) | privacy/page.tsx |
| "যোগাযোগের ক্লিক লগ: ৯০ দিন" | privacy/page.tsx |
| "হ্যাশ করা IP লগ: ৯০ দিন" | privacy/page.tsx |
| "অ্যাডমিন কার্যকলাপ লগ: ১৮০ দিন" | privacy/page.tsx |
| "মুছে ফেলা রক্তদাতা/অনুরোধ রেকর্ড: ১৮০ দিন..." | privacy/page.tsx |
| "সম্পন্ন/বাতিল রক্তের অনুরোধ: ৩৬৫ দিন..." | privacy/page.tsx |
| "দাতার opt-out (তালিকা থেকে লুকানো)" (শিরোনাম) | privacy/page.tsx |
| ২টা দীর্ঘ অনুচ্ছেদ (ফোন নম্বর গোপনীয়তা + ক্লিক লগ) | privacy/page.tsx |
| "গোপনীয়তা নীতি পড়ুন" | CookieBanner.tsx |
| "সঠিক ইমেইল ঠিকানা দিন।" | Newsletter.tsx |
| "অনুগ্রহ করে সব প্রয়োজনীয় তথ্য সঠিকভাবে দিন।" | VolunteerClient.tsx |

**ফিক্স:** এই ১৩টা কী ডিকশনারিতে যোগ করতে হবে (English অনুবাদসহ)।

---

## 🟡 বাগ #2 — অ্যাডমিন UI-তে হার্ডকোড করা বাংলা — **FIXED ✅**

| স্ট্রিং | ফাইল |
|---|---|
| "কোনো দাতা নেই।" | admin/page.tsx (DonutChart empty state) |
| "কোনো বার্তা নেই।" | admin/page.tsx |
| "এখনো কোনো পার্টনার নেই।" | admin/partners/page.tsx |
| "+ QR কোড" | admin/donation-methods/page.tsx |

**ফিক্স:** `tx()` দিয়ে মোড়ানো অথবা ডিকশনারিতে এন্ট্রি।

---

## 🟢 বাগ #3 (মাইনর) — লাইভ রক্তের পপ-আপ মোবাইলে নিচের বাটনগুলো ১৪ সেকেন্ড ঢাকে (ইচ্ছাকৃত UX, ফিক্স নয়)

- **কোথায়:** `LiveRequestAlert.tsx` — `fixed bottom-6 right-4 z-[48]`
- **কী হয়:** মোবাইলে পপ-আপ প্রায় পুরো প্রস্থের (calc(100vw-2rem))। দেখানোর সময় (১৪ সেকেন্ড) বামের 🚨/📞/WhatsApp বাটন ও AI বাটন ঢাকা পড়ে। তবে পপ-আপে নিজেই "এখনই কল" ও "সব দেখুন" বাটন আছে + ক্লোজ বাটন + অটো-বন্ধ — তাই এটা ইচ্ছাকৃত UX, গুরুতর নয়।

---

## সারসংক্ষেপ

- **পাওয়া গিয়েছিল:** 🔴 ১টা (১৫টা মিসিং ট্রান্সলেশন) + 🟡 ১টা (৪টা অ্যাডমিন বাংলা) + 🟢 ১টা (মাইনর, ইচ্ছাকৃত)
- **ঠিক করা হয়েছে:** ✅ বাগ #1 ও #2 (ডিকশনারিতে ১৬টা এন্ট্রি + `tx()` র‍্যাপিং) — মিসিং-স্ক্যানে এখন ০
- **PR #70:** ৩টা কমিট — ব্যানার/মেনু/ওভারলে ফিক্স + ট্রান্সলেশন ফিক্স, MERGEABLE
